import SwiftUI

// Legacy placeholder kept for Xcode compatibility / previews.
// The real app entry point is in WakeupApp.swift using MenuBarExtra.

struct ContentView: View {
    var body: some View {
        Text("Wakeup")
            .padding()
    }
}

#Preview {
    ContentView()
}

